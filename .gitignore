# ============================================================
# 런타임 데이터 / 자격증명 — 절대 커밋 금지 (never commit)
# Runtime data / credentials — must never reach GitHub
# ============================================================
labeling_tool/data/
labeling_tool/config.json
# annotation_tool: default dataset dir, SAM weights, user class definitions
dataset/
checkpoint/
annotation_tool/classes.json
# generic safety net for stray image/session dumps at any depth
**/data/session_*/

# 중국어 README 는 원격 저장소에 올리지 않음 (내부 참고용)
README_zhCN.md

# 가상환경 / 캐시 (virtualenv / caches)
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 로그 (logs)
*.log

# 모델 가중치 (model weights; download per README, too large for git)
labeling_tool/models/sam/*.pt
*.pt
*.pth
*.safetensors
