# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual env
.venv/
venv/

# Model weights (download per README; too large for git)
checkpoint/
*.pt
*.pth
*.safetensors

# User-edited global class definitions (created by the annotation tool UI)
annotation_tool/classes.json

# Runtime data / credentials of the production labeling_tool — never commit.
# (This checkout often holds an untracked labeling_tool/ from main.)
labeling_tool/data/
labeling_tool/config.json
dataset/
**/data/session_*/
